"""Mission builders — one module per mission class.

v3.0 ships `snipe` only. v3.1+ will add `reinforce` (top up friendly
planets under threat), `recapture` (predicted-to-fall planets), and
`gang_up` (multi-source coordinated arrivals).
"""
