# fast_sim microbench — 2026-06-01

> Source: `scripts/bench_fast_sim.py` (seed=42, warmup=30, num=200).
>
> Audit doc generated by the bench script itself; not hand-written. Numbers are wallclock on the box the script ran on.

## Per-step cost

| Path | median (ms) | mean (ms) | p95 (ms) |
|---|---:|---:|---:|
| `env.clone()+step()` | 23.073 | 23.352 | 25.281 |
| `fast_sim.step()` (incl. clone) | 0.107 | 0.113 | 0.137 |
| `fast_sim.clone()` | 0.010 | 0.011 | 0.011 |

**Speedup:** 215.7x over `env.clone()+step()`.

## Full rollout (K=50, v3_snipe self-play)

| | median (ms) | mean (ms) | p95 (ms) | per-step (ms) |
|---|---:|---:|---:|---:|
| `fast_sim.rollout()` | 626.315 | 628.718 | 645.010 | 12.526 |

## What this means for lookahead budget

At a per-turn budget of 1000 ms (`actTimeout`), `env.clone()+step()` allowed 43 step evaluations per turn; `fast_sim.step()` allows 9349 — enough headroom for depth-2 search or PIMC-style opponent sampling.

Phase 2 (`audit/2026-05-11-lookahead-phase2-forward-sim.md`) showed Sim<K=50> reaches AUC 0.952 = oracle; with the new sim budget the same AUC is reachable from many more candidates per real turn.
