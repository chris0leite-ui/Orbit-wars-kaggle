# v12-C3 panel — REGRESSION

Branch: `claude/review-foundations-progress-14HXp` HEAD `8c95407`
Date: 2026-05-19
Test: `python fast.py eval agents/v12 --vs-panel default --max-seeds 8 --workers 4`
Wallclock: 38 min total

## Result

| Opponent | wins/n | winrate | Wilson low | Wilson high | verdict |
|---|---:|---:|---:|---:|---|
| v7_0 | **1/16** | 6.2% | 0.011 | 0.283 | FAIL |
| v4_planner | 9/16 | 56.2% | 0.332 | 0.769 | INCONCLUSIVE |
| v3.5.1 | **3/16** | 18.8% | 0.066 | 0.430 | FAIL |
| **panel verdict** | | | | | **FAIL (worst Wlo=0.011)** |

Focal wallclock OK: p50=584ms, p95=683ms, max=945ms.

## Severity

This is a serious regression vs the architecture and vs the prior
agent. For comparison from prior audits:
- v8_scavenge vs v3.5.1: 75.0% (n=64, Wlo=0.579) — v12-C3 at 18.8%
  is a ~56pp drop
- v8_scavenge vs v7_0: 73.4% — v12-C3 at 6.2% is a ~67pp drop
- v4_planner-bundle submitted at μ=1056 on the ladder; v12-C3 may be
  significantly worse

## Felipe gate WIN doesn't generalize

C3's value-head ship_balance term flipped Felipe-P0 (LOSS→WIN at step
174). But the same change cost 9-12 wins on the panel. The Felipe flip
is real but isolated — the modeling change doesn't compose with the
broader panel of v3.5.1 / v7_0 opponents.

## Hypothesis A — `ship_balance_weight = 0.3` is too aggressive

`evaluate_value_v12` weights `ship_balance` at 0.3. For a leaf state:
- Successful capture: ships move from my-garrison to my-new-planet
  (net ship count up by the production-stream component, minus
  combat losses)
- Bounced launch: ships die outright (net ship count down by full
  launch size)

The chooser may now over-penalize HIGH-RISK launches (where the
worst case is a full bounce). v3.5.1's aggressive snipe pipeline
relies on launching 0.7×src ships at carefully-sized targets;
v12-C3's value head might be reading those launches as "ship
balance dropping" if combat predictions in the K=10 rollout aren't
perfectly accurate.

Test: tune `ship_balance_weight` down to 0.1 or 0.05. If panel
recovers, confirms.

## Hypothesis B — CRN's "me-idle" baseline opp_traj is wrong opponent model

The `record_opp_traj` builds opp's K-step actions assuming I idle.
That gives opp the chance to expand UNOPPOSED for K turns. When I
then score a portfolio where I'm launching, the opp's trajectory
still reflects "v3.5.1 expanding against an idle me" — which is opp's
strongest scenario.

If my action is defensive/aggressive, opp's actual best response
would differ. The CRN simplification (opp's actions are fixed)
may bias the chooser's evaluation toward "actions that look good
against an opp expanding into vacuum."

Test: run v12-C2 (CRN only, original `evaluate_value`) vs v3.5.1 on
4-seed panel. If still bad, CRN is the regression.

## Hypothesis C — Lib drift

Parity check vs `submissions/v4_planner.py` was 0 mismatches on 3
seeds × 20 turns. That's narrow. The libs may diverge later in games
or on uncommon code paths. If `agents/v12` with `evaluate_value`
(original, NOT v12) also regresses panel, the lib drift is the
culprit.

Test: temporarily switch `agents/v12/main.py` to use original
`evaluate_value`, panel-eval. If still bad, lib drift.

## Hypothesis D — v4_planner-bundle ALSO has this problem

The bundle scored μ=1056 on the live ladder, but its head-to-head
performance vs specific top heuristics like v3.5.1 may be similarly
weak. The ladder's TrueSkill is multi-opp; the panel measures
specific head-to-heads.

Test: panel v4_planner-bundle (via `--agent v4_planner` short-name)
vs same 3 opps. If v4_planner-bundle also gets 1/16 vs v7_0 and
3/16 vs v3.5.1, the regression is the architecture, NOT C3.

## Decision

NO SUBMISSION yet — but the picture is more nuanced than panel-FAIL
alone implies.

### Update — Hypothesis D CONFIRMED

`python fast.py eval v4_planner --vs-panel default --max-seeds 8
--workers 4` (the same panel against v4_planner-bundle):

| Opponent | wins/n | winrate | Wlo | Whi | verdict |
|---|---:|---:|---:|---:|---|
| v7_0 | 2/16 | 12.5% | 0.035 | 0.360 | FAIL |
| v3.5.1 | 2/16 | 12.5% | 0.035 | 0.360 | FAIL |
| (v4_planner self-skipped after early-stop) | — | — | — | — | — |

**v4_planner-bundle ALSO fails this panel head-to-head, comparable
to v12-C3.** Yet v4_planner-bundle scored μ=1056 on the live ladder
(20 μ higher than v8's 1043). The panel weakness is intrinsic to
the v4_planner architecture family — NOT a v12 regression.

### Implication: local-panel under-predicts ladder for this arch

The plan's verification mandates panel Wlo ≥ 0.55. For v4_planner-
family agents, even the bundled live-tested μ=1056 submission would
fail that gate. The gate is too strict for this architecture (or
calibrated to v3_snipe / v8-style chooser-based agents).

This is an important calibration finding for next session:
**don't rely on panel-Wlo against v3.5.1 / v7_0 to predict ladder
performance for portfolio-search agents.** The panel measures
head-to-head; the ladder measures TrueSkill across the full
distribution.

### Likely cause: noop-portfolio bias under symmetric matches

v4_planner's 5 portfolios include `noop` (empty mission list).
Against equal-tier opponents (v3.5.1, v7_0), the K-step rollout
under v3.5.1-as-opp-policy produces near-symmetric leaf states.
With noop, my action is "do nothing"; opp launches; my prod
catches up by step 10. With incumbent (active), my snipe partially
collides with opp's snipe; expected captures often bounce; ship
balance worsens.

evaluate_value_v12's ship_balance term EXAGGERATES this — noop's
"preserved ships" beat incumbent's "launched-and-bounced" ships
in the leaf-state ship_balance reading. Chooser picks noop too
often.

### Revised next steps

1. **Verify by counting portfolio selections in a panel game.**
   Add a 5-line debug print to `agents/v12/main.py` showing which
   portfolio's `best_v` won, per turn. If it's consistently `noop`
   or `conservative`, that confirms passivity bias.

2. **Drop or down-weight `noop` portfolio.** Remove from
   `lib/candidate_portfolios.py::generate_portfolios` for v12, or
   add a `passivity_penalty` to evaluate_value_v12 that subtracts
   from leaf-favor when my fleet count is zero.

3. **Submit v12-C3 to ladder regardless of panel.** The
   v4_planner-bundle data point shows panel is wrong calibration
   for this arch family. Live ladder μ is the actual measurement.
   Live downside risk is bounded (rolling-last-2 keeps v9 at 1085;
   v12 submission only evicts v8 at 1043).

4. **C5 K_MAX bump is now more relevant.** Longer K means more
   game-evolution at the leaf, less symmetric, ship_balance
   becomes a more useful signal. May break the noop-bias.

### Submission risk-reward calculation

Risk: v12-C3 ladder μ < 1043 → evicts v8, team floor drops from
v9=1085 down to v12=(whatever). Plausibly the panel REGRESSION
(C3 vs v4_planner: 1/16 vs 2/16 v7_0; 3/16 vs 2/16 v3.5.1) is
within noise.

Reward: v12-C3 ladder μ > 1085 → new high; v8 was already
recoverable since we have v9 in rolling-last-2.

Recommendation: **PI decides**. If we have confidence (Felipe flip
is real evidence the modeling improvement works at game level), one
submission slot is worth it. The 5/day budget is plenty for one
experiment.

## What this tells us about the methodology

The "look at games to improve modeling" loop worked at the GAME level
(Felipe forensic → ship-balance hypothesis → Felipe flip). But it did
NOT verify that the fix composed with the broader panel. A
single-seed win doesn't generalize without a panel gate.

The plan's verification step Gate 5 (panel) is what's catching this
correctly. Don't ship before the panel is green.

## Reproduction

```
python fast.py eval agents/v12 --vs-panel default --max-seeds 8 --workers 4
```

Took 38 min. Single-thread game ~2.5 min. Bottlenecked by both agents
running full K-step rollouts.
