# Open question — next-axis direction

After 10 slices of analytical-chooser work below trajectory parity,
where does the next session's effort go? Three plausible directions:

1. **Analytical leaf eval INSIDE the existing trajectory rollout**
   — replace `lite_greedy + favor` leaf with differential's
   closed-form projection. Keeps the rollout's planning role;
   swaps its substrate. Cheaper than building multi-turn LP.

2. **Trajectory-side improvements** — better opp policy
   (top_tier_mirror vs lite_greedy), leaf-eval World/Model
   cache, value head A/B with new heads.

3. **Submission strategy** — 33 days to deadline. Marginal hour
   on opp-class fingerprinting, late-window submission timing,
   rolling-pair management rather than architecture.

Question for PI: which axis next session, or some combination?
