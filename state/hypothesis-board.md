# state/hypothesis-board.md — open agent-design hypotheses

## Open

> Day-1 agent: seed 3-5 hypotheses after reading the comp brief and
> reference notebooks. Examples:
> - H1: A search-based agent (MCTS over short horizons) beats a
>   hand-coded heuristic on the baseline-opponent panel.
> - H2: An RL agent trained on self-play overfits to symmetric
>   strategies and loses to rule-based opponents — needs opponent-
>   curriculum diversity.
> - H3: Replay statistics from top public-LB agents reveal a
>   load-bearing tactic that no public notebook documents.

## Killed

(empty — append entries when a hypothesis is falsified.)

## Hedge ladder

> Per CLAUDE.md R2: PRIMARY = best-current; HEDGE = next-best agent
> that regressed ≤ defined-bracket on the rank ladder. Populate during
> the final 3-day window.
