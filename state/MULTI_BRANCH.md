# state/MULTI_BRANCH.md — push-claim board (Rule 42)

> Slimmed 2026-06-03 to the push-claim board only. The pre-strategy-lock
> version (track registry, closed tracks, per-branch sync table, substrate
> tier split) is preserved at `state/_archive/MULTI_BRANCH-pre-adaptive-k.md`.

## Live Kaggle — read directly

Scores (μ) and the rolling pair are NEVER transcribed here — they go stale
within hours. The leaderboard is the single source of truth:

```
kaggle competitions submissions orbit-wars
```

- **Rolling pair** = the **two most-recent** submissions in that list
  (Kaggle auto-keeps exactly these two for final evaluation).
- **Daily submission budget:** 5/day.
- **Deadline:** 2026-06-23 23:59 UTC (fixed).

Before any submit, fill the push-claim board below using the freshly-read pair.

## Push-claim board (Rule 42 — fill before every `kaggle competitions submit`)

Most recent claim at top. Required fields: all 6 columns. The PI-signoff
column is mandatory if evicted-μ > predicted-μ (Rule 42).

| Timestamp (UTC) | Branch | Agent | Predicted μ | Will evict (sub_id, μ) | PI signoff |
|---|---|---|---:|---|---|
| 2026-06-10 06:54 | `claude/awesome-clarke-ixy57v` | `producer_plus_ffa_uniform_on.py` (283 684 B, sha256 `0ba42ce82bbc...`). The proven multi_opp_def stack + the FFA objective fix with uniform rival weights (`PRODUCER_PLUS_FFA_SCORE=1`, `PRODUCER_PLUS_FFA_WEIGHTS=uniform`): in 3+ player games the opponent term of competitive_score becomes an equal-weight AVERAGE over living rivals instead of the SUM, so mutual-damage trades score negative while profitable captures keep full value. Motivation: live-replay diagnosis — 4P is 60% of ladder volume, our 4P winrate 29% vs 63% 2P, 82/83 4P losses end eliminated by 2+ opponents carving us after trades. **2P behavior is byte-identical to the live champion** (weights only built when player_count >= 3; action-stream equality test green). | ≈ 1230-1330 if the 4P lift translates (4P = 60% of games); floor ≈ multi_opp_def's level since 2P is identical and 4P-vs-producer is parity-or-better. Evidence: 4P first-place vs 3x vanilla producer = **14/32 = 43.8%** (baseline multi_opp_def 13/32 = 40.6%, same seeds); 4P vs 3x our-own-best pool = **17/48 = 35.4% outright wins where the identical-agent control scores 0/24** (self-play stalemates to step 500 — the variant breaks the cycle stalemate and wins by ship count or elimination in 100-200 steps). Rule 45 note: 4P-axis claim measured in 4P; 2P axis risk-free by byte-parity. | **53450504** `producer_plus_multi_size_on` μ = 1181.1 (older half of rolling pair). Backstop position 2: **53523036** `producer_plus_multi_opp_def_on` (restore, warming: 1035 -> 1247 -> 1148 readings; settle ETA 2026-06-11 ~04:00 UTC) stays as new #2. Evicted-μ 1181.1 < predicted floor ≈ multi_opp_def level → **Rule 42 GREEN**. | ✅ PI explicit sign-off via session question (2026-06-10 07:06 UTC); **submitted as sub 53527125** (2026-06-10 07:07 UTC). Rule 46 GREEN: bundle parses, bakes 4 env vars; tests/test_bundle.py 15/15; idle-CPU smoke vs v7_0_drop_one seed 7 = win, 113 steps, p50=53 ms, max=76 ms << 1000 ms; 11/11 FFA unit tests. |
| 2026-06-09 21:26 | `claude/awesome-clarke-ixy57v` (majestic-storm track merged at `2cc6d1c`; bundle rebuilt from current HEAD) | `producer_plus_multi_opp_def_on.py` **RESTORE** (279 149 B, sha256 `4d51456806a5...`). Identical mechanism to evicted sub 53384340 (multi_size + Producer-mirror opp projection + opp-aware defensive shortlist; coalitions OFF, recapture OFF, multi-tick OFF, force-concentration OFF). Bundle is larger than the 06-05 build only because the engine now carries the default-OFF recapture / strategic-value / force-concentration code; OFF-parity covered by test_producer_plus_opp_proj.py::test_off_path_bit_identical_to_producer (GREEN today). Context: the 06-07 no-description resubmit of multi_size (sub 53450504, settled 1181.1) evicted our best agent multi_opp_def (settled 1263.4-1286.8); the rolling pair floor is now tick_recap's 1099.3. All three force-concentration variants A/B'd today regressed hard vs producer (6/32, 7/32, 5/32) — negative result banked; restoring the known-best agent is the highest-expected-μ move. | ≈ 1180-1260 (settled 1263-1286 on 06-05; the identical-code multi_size resubmit data point 1282→1181 says the field strengthened ~100 μ in 3 days, so predict conservatively). Fresh local **n=32 seat-balanced clean A/B vs vanilla producer today = 24/32 = 75.0%, Wilson [0.579, 0.867]** — exactly reproduces the 06-05 result on the rebuilt bundle. Rule 45 GREEN (Wilson-lo 0.579 ≥ 0.55). | **53390700** `producer_plus_multi_tick_recap_on` μ = 1099.3 (older half of rolling pair). Backstop position 2: **53450504** `producer_plus_multi_size_on` μ = 1181.1 (stays as new #2). Evicted-μ 1099.3 << predicted ≈ 1180-1260 → **Rule 42 GREEN** (~80-160 μ rolling-pair floor lift). | ✅ PI explicit sign-off via session question (2026-06-10 ~04:10 UTC); **submitted as sub 53523036** (2026-06-10 04:12 UTC). Rule 46 GREEN: build script wrote bundle; tests/test_bundle.py 15/15; `fast.py play` vs v7_0_drop_one seed 7 → p0_win 113 steps, p50=62 ms, max=89 ms << 1000 ms cap. |
| 2026-06-05 11:05 | `claude/champion-ml-graft-majestic-storm` (commit `68604a6` on top of `173c952`) | `producer_plus_multi_tick_recap_on.py` (272 870 B, sha256 `049915a4ecdf...`). Multi_size + opp_proj + multi-tick (K_4P=3, K_2P=2) + recapture penalty. Built on the multi_opp_def base (sub 53384340 μ=1286.8) by adding two scorer corrections: (a) K-round opp projection so the scorer sees opp's near-term actions across game-ticks 0..K-1 instead of just tick 0; (b) recapture penalty subtracting `prod[T] × turns_lost` in ship units when opp can plausibly retake the captured planet — discourages thin captures. K_recap_eff = max(1, K_recap - K_opp) clips the recap window to past what multi-tick already modeled, avoiding double-counting. Strategic-value bonuses (denial + opening) NOT baked here — they regressed 0/4 in a clean A/B vs producer because the default weights were too aggressive (bonuses ~2-10× the typical competitive_score magnitude); deferred for re-tuning. | ≈ 1280-1320 (modest optimism: builds on multi_opp_def's 1286.8 base; expected lift from multi-tick + recap is small but positive). **n=16 2P A/B vs vanilla producer = 8/16 = 50%** under 2-worker CPU contention (p50=1100ms, max=3.2s — likely timeout-forfeits inflate variance). Wilson-lo 0.28 strictly fails Rule 45's 0.50 gate; PI explicit override. Single-process smoke clean (p50=85ms, max=128-165ms). Mechanism targets 4P cycle stalemate; 2P A/B is the wrong arena (the 4P self-match cycle diagnostic ran 1 hour CPU without finishing, almost certainly hit step cap = cycle persists, but that's measured separately from whether multi-tick+recap LIFTS μ on the live ladder which is diverse). | **53373322** `producer_plus_coalitions_on` μ = 1266.6 (older half of rolling pair). Backstop position 2: **53384340** `producer_plus_multi_opp_def_on` μ = 1286.8 (stays as new #2). Evicted-μ 1266.6 < predicted ≈ 1290 → **Rule 42 GREEN** (~20-50 μ rolling-pair floor lift expected). | ✅ PI explicit "submit multitick recap and bring it to the front" (2026-06-05 11:05 UTC). Rule 45 explicit override (n=16 Wilson-lo 0.28 < 0.50 strict gate; 2-worker contention contaminated the measurement). Rule 46 GREEN: bundle 272 870 B parses + bakes 4 env vars (PRODUCER_PLUS_MULTI_SIZE, PRODUCER_PLUS_OPP_PROJECTION, PRODUCER_PLUS_MULTI_TICK_OPP_K_4P=3, PRODUCER_PLUS_MULTI_TICK_OPP_K_2P=2, PRODUCER_PLUS_RECAPTURE_PENALTY) / smoke `fast.py play` vs v7_0_drop_one seed 7 → p0_win 122 steps, max turn under 200ms (well under 1000ms cap). Rule 38 OFF parity GREEN by construction. 106/106 producer_plus fast tests pass. |
| 2026-06-05 10:50 | `claude/champion-ml-graft-majestic-storm` (commit `8c0c228` on top of `173c952`) | `producer_plus_multi_tick_strategic_on.py` (273 311 B, sha256 `0f53379407...`). Composed bundle: multi_size + opp_proj + multi-tick (K_4P=3, K_2P=2) + recapture penalty + denial bonus + opening bonus. Builds on the multi_opp_def mechanism (sub 53384340 settled at μ=1286.8) by adding three new scorer terms: (a) multi-tick opp projection — opp's planner now runs K rounds per turn so the scorer sees opp's near-term actions across game-ticks 0..K-1 instead of just tick 0; (b) recapture penalty — subtracts a discount from candidates whose post-capture defender is below opp's plausible recapture force, in ship units of (prod × turns_lost); (c) strategic-value bonuses — denial (rewards captures of targets opp values, encoding "block opp's biggest bet") + opening (early-game phase boost for the horizon-too-short defect). All four mechanisms compose additively; recap and the bonuses share a `_compute_captures()` helper. K_recap_eff = max(1, K_recap - K_opp) avoids double-counting multi-tick's window. | ≈ 1290-1330 (modest optimism: builds on multi_opp_def's 1286.8 base; expected lift from the four added mechanisms is small but positive). **n=16 2P A/B vs vanilla producer = 8/16 = 50%, Wilson [0.28, 0.72] — INCONCLUSIVE under 2-worker CPU contention** (p50=1100ms, max=3.2s — likely many timeout-forfeits inflate the variance). **Rule 45 strictly FAILS** (n=16 Wilson-lo 0.28 < 0.50). The strategic mechanisms are 4P-cycle-targeted; 4P self-match diagnostic running at submit time. Single-process smoke shows clean wallclock (p50=89ms, max=316ms, < 1000ms cap) so the timeout contamination is purely the 2-worker A/B harness. | **53373322** `producer_plus_coalitions_on` μ = 1266.6 (older half of rolling pair). Backstop position 2: **53384340** `producer_plus_multi_opp_def_on` μ = 1286.8 (stays as new #2). Evicted-μ 1266.6 < predicted ≈ 1290 → **Rule 42 GREEN** (~20-60 μ rolling-pair floor lift if predicted holds). | **AWAITING PI SIGN-OFF.** Rule 45 strictly fails (n=16 Wilson-lo 0.28 < 0.50 gate) — explicit PI override required. Rule 46 GREEN: bundle 273 311 B parses + bakes all 7 env vars / smoke `fast.py play submissions/producer_plus_multi_tick_strategic_on.py --vs submissions/v7_0_drop_one.py --seed 7` → p0_win 118 steps, p50=89ms, p95=112ms, max=316ms (well under 1000ms cap, 4 steps shorter than multi_tick_recap). Rule 38 OFF parity GREEN by construction (every new gate defaults OFF; existing test_producer_plus_opp_proj.py::test_off_path_bit_identical_to_producer covers byte-identity). 106/106 producer_plus fast tests pass; 55/55 strategic_value fast tests pass. |
| 2026-06-05 06:55 | `claude/champion-ml-graft-majestic-storm` (commit `a9af667` on top of `de8cddc`) | `producer_plus_multi_opp_def_on.py` (241 724 B). New variant: `PRODUCER_PLUS_MULTI_SIZE=1` + `PRODUCER_PLUS_OPP_PROJECTION=1`, with the Approach A opp-aware defensive shortlist baked into `friendly_flip_targets` (augments its flip set from the opp_proj background — the defensive lane now reacts to opp's predicted attacks, not only to fleets already in flight). Coalitions deliberately OFF (kitchen-sink diagnostic at seed 7 showed coalitions barely fire and net -1 win vs no-coalitions). Opponent model is Producer-mirror (`predict_opp_launches_via_mirror`): runs Producer's own planner from each opp seat with `background=None` and feeds its launches as the scorer's background LaunchSet. roi_threshold is renormalized per turn by do_nothing_score (`_score_do_nothing` helper) to preserve "fire only if you gain ≥ 1.5 ships over not firing" semantics under opp-aware scoring. | ≈ 1295-1325. Local **n=32** seat-balanced vs producer = **24/32 = 75 %**, Wilson 95% CI [0.58, 0.87]. **Wilson-lo 0.58 clears Rule 45's 0.55 gate** (and 0.50 strict gate). Per-seat: 10W/6L P0, 14W/2L P1 — opp_proj+def especially helps P1 seat (early tempo recovery via reactive defense). Sibling `producer_plus_multi_size_on` (sub 53369848, μ ≈ 1282) and `producer_plus_coalitions_on` (sub 53373322, μ ≈ 1263) both peak at the static-opp-scorer plateau; the +2-3 win local lift vs multi_size at the same 16 seeds projects to ≥ +15 μ on the ladder. | **53369848** `producer_plus_multi_size_on` μ = 1282.1 (older half of rolling pair — submitted 2026-06-04 17:29). Backstop position 2: **53373322** `producer_plus_coalitions_on` μ = 1262.6 (stays as new #2). Evicted-μ 1282.1 < predicted ≈ 1310 → **Rule 42 GREEN** (~30 μ rolling-pair floor lift). | ✅ PI explicit "submit it already to kaggle and then kick off the thirty two seeds test" (2026-06-05). Rule 45 GREEN (n=32 Wilson-lo 0.58 clears 0.55). Rule 46 GREEN: bundle 241 724 B parses + bakes `PRODUCER_PLUS_MULTI_SIZE=1` and `PRODUCER_PLUS_OPP_PROJECTION=1` / smoke `fast.py play submissions/producer_plus_multi_opp_def_on.py --vs submissions/v7_0_drop_one.py --seed 7` → OUR side max turn 73 ms << 1000 ms cap (v7_0's side hit 1025 ms — v7_0 TLE, not ours). Rule 38 OFF parity GREEN by construction (Approach A augmentation skips when `background is None` or empty; existing `tests/test_producer_plus_opp_proj.py` covers this). |
| 2026-06-04 19:55 | `claude/champion-ml-graft-majestic-storm` (commit `193921e` on top of `7939bad`) | `producer_plus_coalitions_on.py` (229 448 B). Step 5 Fix-A: L=2 multi-source coalitions with **disjoint-coverage gate** — fire only when `neither_alone & joint_clears & eta_strict & distinct_src`. Multi-size (Step 4) deliberately OFF: serial n=16 seat-balanced composed = 9/16 (regression) because multi-size LO/MID variants under-send and producer reactively re-captures (static-opp scorer defect; investigation `audit/2026-06-04-...`). Coalitions only use safe_drain per contributor, full-drain sends, scorer-defect-immune. Overnight A/B vs the warming `producer_plus_multi_size_on` (sub 53369848 currently μ ≈ 1303) tests whether Fix-A is closer-to / further-from / equal-to multi-size on the live ladder. | ≈ 1280-1310 (sibling mechanism on producer engine; local n=32 vs producer = 20/32 = 62.5 %, Wilson [0.45, 0.77], identical to multi_size local 10/16 = 62.5 %). Rule 45 0.55 Wilson-lo gate not cleared (lo = 0.45) — PI explicit "submit our currently best solution as an overnight A/B test" sign-off. | **53336920** `champ_refine_adaptivek` μ = 1146.1 (older half of rolling pair). Backstop position 2: **53369848** `producer_plus_multi_size_on` μ ≈ 1303 (warming up, stays as new #2). Evicted-μ 1146.1 < predicted ≈ 1290 → **Rule 42 GREEN** (~140 μ rolling-pair floor lift). | ✅ PI explicit "submit our currently best solution as an overnight A/B test on kaggle" (2026-06-04 19:55 UTC). Rule 45 explicit override (n=32 Wilson-lo = 0.45 < 0.55 gate; PI accepts as overnight A/B vs sub 53369848). Rule 46 GREEN: bundle 229 448 B parses + bakes ONLY `PRODUCER_PLUS_COALITIONS=1` / `fast.py play submissions/producer_plus_coalitions_on.py --vs submissions/v7_0_drop_one.py --seed 7` → P0 win 116 steps, max turn 107 ms. Rule 38 OFF parity GREEN (`tests/test_producer_plus_coalitions.py::test_off_path_bit_identical_to_producer`). |
| 2026-06-04 17:30 | `claude/champion-ml-graft-majestic-storm` (commit `4005b19` on top of strategy-lock commit `ccec9f8`) | `producer_plus_multi_size_on.py` **submitted sub 53369848** (211 011 B). First submission from the **producer_plus migration track**: Producer's engine (Slawek Biel's published `producer` agent, MIT-licensed) hosting OUR new mechanism — multi-size candidate enumeration per (source, target) pair (`PRODUCER_PLUS_MULTI_SIZE=1`). Three ship-size variants per pair (capture_floor, 2×floor, safe_drain) folded into the candidate axis (`C = S × T × 3, L = 1`), greedy target-mutex picks the best variant. Adaptive K (Step 2) deliberately NOT baked in: 16-game seat-alt A/B vs producer showed it at exactly 8/16 parity. | ≈ 1200-1250 (rough). Local 16-game seat-alternated A/B vs vanilla producer (live μ ≈ 1200): **10 wins / 6 losses = 62.5 %**, Wilson [0.39, 0.81]. **Perfectly seat-balanced** (5W/3L on both P0 and P1). Per-seat split visible in `state/MULTI_BRANCH.md` row commentary. **n=16 alone does NOT clear Rule 45's 0.55 Wilson-lo gate** — PI explicit override. | **53332500** `champ_computeByShips_on` μ = 1150.6 (older half of rolling pair). Backstop position 2: **53336920** `champ_refine_adaptivek` μ = 1148.8 (stays in rolling pair as new #2). Evicted-μ 1150.6 < predicted 1200 → **Rule 42 GREEN** (~50 μ rolling-pair floor lift). | ✅ PI explicit "let's submit" (2026-06-04). Rule 45 explicitly overridden — n=16 sub-gate sample size. Rule 46 GREEN: bundle 211 011 B parses + bakes ONLY `PRODUCER_PLUS_MULTI_SIZE=1` / `fast.py play submissions/producer_plus_multi_size_on.py vs producer seed=7` → P0 win 129 steps, max turn 138 ms (well under 1000 ms cap). |
| 2026-06-03 15:11 | `claude/champion-ml-graft-majestic-storm` (commit `0025c67`, on top of strategy-lock commit `ccec9f8`) | `champ_computeByShips_on.py` **submitted sub 53332500** (sha256 `53bf813b...`, 697 927 B). Same all-time-champion config + adaptive horizon K + **NEW: compute_by_ships lever** (`BASELINE_COMPUTE_BY_SHIPS=1`). Lever does two things proportional to a planet's ship surplus: (1) enumerates 4..16 target options per planet (log-scaled vs avg fleet), (2) raises the launch-arrival K cap by up to +50% for high-ship planets. Both default OFF; champion byte-identical when env unset. | ≈ 1170 (parity with adaptive_k sibling; local 8-seed/16-game A/B vs same-source lever-off = 7/16 wins, Wilson [0.231, 0.668] — INCONCLUSIVE, point estimate slightly below parity but CI overlaps both sides). Diagnostic probe showed mechanism is NOT activating the original rear-stockpile case (zero launches from sources >150 dist to opp in either bundle), but lever DOES shift the launch mix in 100% of games. Submitting to test against the broader Kaggle opponent panel (not just sibling). | **53316984** `baseline_state_k_orbital_lead` μ = 1114.1 (older half of rolling pair). Backstop position 2: **53324164** `champ_adaptiveK_on` μ = 1185.2 (our current live champion — stays in the rolling pair as new #2). Evicted-μ 1114.1 < predicted 1170 → **Rule 42 GREEN**: rolling-pair floor rises ≈ 56 μ; live champion NOT at risk. | ✅ PI explicit "submit" (2026-06-03 15:11 UTC). Rule 46 GREEN: bundle 697 927 B parses + has lever + has no cross-agent imports / `tests/test_bundle.py` 15/15 / `fast.py play` vs `v7_0` seed = 7 → won 192 steps, max turn 744 ms. Unit tests `tests/test_compute_by_ships.py` 12/12. |
| 2026-06-03 10:37 | champion-strategy-rules-00JzI (built via worktree at commit `9985e98` from the `claude/champion-ml-graft-majestic-storm` session) | `baseline_adaptive_k` **resubmit** (sub **53324164**, bundle sha256 `6c0419dc20`, 608 844 B) — champion (`launch_rules_universal`) full config + adaptive horizon K baked ON (`BASELINE_ADAPTIVE_K = 1`, K_OPEN = 20 → floor 10 by step 30). Same agent as evicted sub 53265480 which settled at live μ = 1170.4. Designated as the **main strategy** going forward (`state/STRATEGY.md`). | ≈ 1170 based on prior live settle of identical agent. High confidence (same code; only stochastic ladder noise). | **53304016** `baseline_launch_rules_universal` μ = 1131.1 (older half of rolling pair; was itself a 2026-06-02 21:09 resubmit of the all-time-champion bundle that did NOT reach its historic 1183.7 peak — settled 52 μ below). Backstop position 2: **53316984** `baseline_state_k_orbital_lead` μ = 1109.0 (still settling, ~4 h on ladder at submit time). Evicted-μ 1131.1 < predicted 1170 → **Rule 42 GREEN**: rolling-pair floor rises ≈ 40 μ. | ✅ PI explicit "research submit this solution: baseline_adaptive_k …" (2026-06-03). Rule 46 GREEN: bundle 608 844 B / `tests/test_bundle.py` 15/15 / `fast.py play` vs `v7_0` seed = 7 → 281 steps p0_win, max turn 639 ms. |

## Pointers

- `state/STRATEGY.md` — the strategy itself.
- `CLAUDE.md` — process rules (incl. Rule 42 gate semantics).
